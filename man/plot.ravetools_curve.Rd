% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve-catmull.R
\name{plot.ravetools_curve}
\alias{plot.ravetools_curve}
\title{Plot method for \code{ravetools_curve}}
\usage{
\method{plot}{ravetools_curve}(x, n = 200L, col = "steelblue", pch = 19L, cex = 1, use_rgl = TRUE, ...)
}
\arguments{
\item{x}{an object of class \code{ravetools_curve}.}

\item{n}{integer; number of sample points used to draw the smooth curve.
Default is \code{200L}.}

\item{col}{color for the spline curve line. Default \code{"steelblue"}.}

\item{pch, cex}{plotting character and scaling for the key control points
(base-R fallback only). Default \code{pch = 19}, \code{cex = 1}.}

\item{use_rgl}{logical; if \code{TRUE} (default) and the \code{rgl}
package is installed, an interactive 3D window is used. Set to
\code{FALSE} to force the base-R 2D projection panels.}

\item{...}{additional graphical parameters forwarded to the underlying
plot calls.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plots a \code{ravetools_curve} object created by
\code{\link{catmull_rom_3d}}.  When the \code{rgl} package is available
and \code{use_rgl = TRUE} (default), an interactive 3D scene is opened.
Otherwise three 2-D projection panels (\emph{x-y}, \emph{x-z},
\emph{y-z}) are drawn using base \R{} graphics.
}
