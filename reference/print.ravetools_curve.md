# Print method for `ravetools_curve`

Prints a concise summary of a `ravetools_curve` object returned by
[`catmull_rom_3d`](https://dipterix.org/ravetools/reference/catmull_rom_3d.md).

## Usage

``` r
# S3 method for class 'ravetools_curve'
print(x, ...)
```

## Arguments

- x:

  an object of class `ravetools_curve`.

- ...:

  additional arguments passed to
  [`print.data.frame`](https://rdrr.io/r/base/print.dataframe.html).

## Value

Invisibly returns `x`.
